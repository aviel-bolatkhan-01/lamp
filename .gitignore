.DS_Store
.firebase/
*.backup
*.bak
/tmp/

# Claude Code internals
.claude/
entities.json
mempalace.yaml

# AI key — served by Firebase Hosting, NEVER in the GitHub repo
# (Google auto-disables Gemini keys found in public repos, even restricted ones)
ai-key.js

# Session test harness (transient)
_*.py
_*.html
_res*.jsonl
_srv.log

# Android signing — NEVER commit keys or passwords
android/*.keystore
android/*.jks
android/keystore*
android/signing-info.txt

# Android build outputs (rebuildable; keep out of git)
android/*.aab
android/*.apk
android/*.idsig
android/build/
android/app/build/
android/.gradle/
